# Schema

Base de datos principal del sistema multi-bodega.

## Archivos relevantes

- `initial-domain.sql`: referencia inicial del modelo SQL existente
- `inventory-mvp.md`: modelo logico del MVP de inventario para API/DB

## Estado al 17-03-2026

`initial-domain.sql` sigue sirviendo como referencia legible del dominio. Su contenido ya esta reflejado en `db/migrations/0001_inventory_mvp.sql`.
