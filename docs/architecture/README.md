# Architecture

Documentacion tecnica del backend, frontend, integraciones e infraestructura.

## Documentos relevantes para este ciclo

- `backend-modules.md`: mapa de modulos del backend y su estado
- `api-db-inventory-mvp.md`: diseno tecnico aprobado para el MVP de API y DB

## Alcance del ciclo actual

En este ciclo se documenta solo el trabajo de `apps/api` y `db`. Cualquier referencia a frontend o infraestructura queda fuera de alcance salvo menciones contextuales.
